// initial state of every global react state
// import cookie from 'js-cookie';

export default {
  routes: {
    isFetching: true,
    data:[
      { path: '/dashboard',title: 'Home' },
    ]
  }
};
